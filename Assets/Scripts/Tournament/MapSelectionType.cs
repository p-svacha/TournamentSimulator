using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum MapSelectionType
{
    /// <summary>
    /// Map is chosen randomly from the map pool for each game. Non-repeating within a match
    /// </summary>
    Random
}
